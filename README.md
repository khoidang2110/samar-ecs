# CI/CD với AWS ECS Fargate

Deploy ứng dụng web tự động lên AWS ECS khi push code lên GitHub.

## Tech Stack

- **AWS ECS Fargate**: Chạy container không cần quản lý server
- **Application Load Balancer**: Phân phối traffic
- **ECR**: Lưu Docker image
- **Terraform**: Tạo infrastructure tự động
- **GitHub Actions**: CI/CD pipeline

## 🏗️ AWS Services Cần Triển Khai

```
┌─────────────────────────────────────────────────────────────┐
│                    AWS Infrastructure                       │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  1. VPC (Virtual Private Cloud)                     │    │
│  │     └─ Network cơ sở: 10.0.0.0/16                   │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  2. Subnets                                         │    │
│  │     ├─ Public Subnets x2 (cho ALB)                  │    │
│  │     └─ Private Subnets x2 (dự phòng)                │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  3. Internet Gateway + NAT Gateway                  │    │
│  │     └─ Kết nối internet cho VPC                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  4. ECR (Elastic Container Registry)                │    │
│  │     └─ Lưu trữ Docker images                        │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  5. ECS (Elastic Container Service)                 │    │
│  │     ├─ Cluster: khoi-ecs-cluster                    │    │
│  │     ├─ Task Definition: khoi-task                   │    │
│  │     └─ Service: khoi-service (Fargate)              │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  6. ALB (Application Load Balancer)                 │    │
│  │     ├─ Listener: HTTP Port 80                       │    │
│  │     └─ Target Group: Route to ECS tasks             │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  7. Security Groups                                 │    │
│  │     ├─ ALB SG: Allow 80, 443 from internet          │    │
│  │     └─ ECS SG: Allow 80 from ALB only               │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  8. IAM (Identity and Access Management)            │    │
│  │     ├─ Task Execution Role (pull image, logs)       │    │
│  │     ├─ Task Role (app permissions)                  │    │
│  │     └─ User: github-actions-deployer (CI/CD)        │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  9. CloudWatch Logs                                 │    │
│  │     └─ Log Group: /ecs/khoi-task                    │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                 │
│                           ▼                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  10. Auto Scaling                                   │    │
│  │      ├─ Target Tracking: CPU 70%, Memory 80%        │    │
│  │      └─ Min: 1 task, Max: 10 tasks                  │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Luồng CI/CD

```
┌─────────────┐
│ Developer   │
│ Push Code   │
└──────┬──────┘
       │
       │ git push
       ▼
┌─────────────────────────────────────────────────────────┐
│               GitHub Repository                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │  .github/workflows/deploy.yml                     │  │
│  └───────────────────────────────────────────────────┘  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   │ Trigger
                   ▼
        ┌──────────────────────┐
        │   GitHub Actions     │
        │   (Workflow Start)   │
        └──────────┬───────────┘
                   │
    ┌──────────────┴───────────────┐
    │                              │
    ▼                              ▼
┌────────────────┐          ┌──────────────┐
│  Build Job     │          │  Deploy Job  │
│ (Self-hosted)  │          │  (GitHub)    │
└────┬───────────┘          └──────┬───────┘
     │                             │
     │ 1. Checkout code            │
     │ 2. Login ECR                │
     │ 3. Build image              │
     │ 4. Tag: latest, sha         │
     │ 5. Push to ECR ────┐        │
     │                    │        │
     ▼                    ▼        │
┌──────────────────────────────┐   │
│         Amazon ECR           │   │
│  ┌────────────────────────┐  │   │
│  │ Image: latest          │  │   │
│  │ Image: 20251120-abc123 │  │   │
│  └────────────────────────┘  │   │
└──────────────┬───────────────┘   │
               │                   │ 6. Update service
               │                   │ 7. Force deploy
               │                   ▼
               │         ┌───────────────────┐
               │         │   ECS Service     │
               │         │  (khoi-service)   │
               │         └─────────┬─────────┘
               │                   │
               └──────────┐        │
                          │        │
                  Pull Image       │ Blue/Green Deploy
                          │        │
                          ▼        ▼
              ┌────────────────────────────┐
              │      ECS Tasks             │
              │  ┌──────┐      ┌──────┐    │
              │  │ Old  │ ──▶  │ New  │    │
              │  │ v1.0 │      │ v1.1 │    │
              │  └──────┘      └──────┘    │
              │     │              │       │
              │   Stop          Start      │
              └─────┼──────────────┼───────┘
                    │              │
                    └──────┬───────┘
                           │ Register
                           ▼
                    ┌──────────────┐
                    │  ALB Target  │
                    │    Group     │
                    └──────┬───────┘
                           │
                           │ Route Traffic
                           ▼
                    ┌──────────────┐
                    │    Users     │
                    │  (Zero Down) │
                    └──────────────┘
```

## ⚙️ Infrastructure Components (Terraform)

```
terraform/
├── vpc/                    ← Tạo VPC, Subnets, IGW, NAT
│   └── main.tf
│
└── ecs/                    ← Tạo ECS, ALB, IAM, ECR access
    ├── main.tf            (Cluster, Service, Tasks)
    ├── iam.tf             (Roles, User cho GitHub)
    └── terraform.tfvars   (Config: ECR URL, task size)
```

## Cách triển khai

### Bước 1: Tạo Infrastructure với Terraform

```bash
# Tạo VPC
cd terraform/vpc
terraform init
terraform apply

# Tạo ECS (sửa ECR URL trong terraform.tfvars trước)
cd ../ecs
terraform init
terraform apply

# Lấy thông tin
terraform output alb_dns_name                    # URL ứng dụng
terraform output github_actions_access_key_id     # AWS Key
terraform output github_actions_secret_access_key # AWS Secret
```

### Bước 2: Setup GitHub Secrets

Vào **GitHub → Settings → Secrets → Actions**, thêm:
- `AWS_ACCESS_KEY_ID`
- `AWS_SECRET_ACCESS_KEY`
- `AWS_REGION` = `ap-southeast-2`

### Bước 3: Setup Build Server (Self-hosted Runner)

Vào **GitHub → Settings → Actions → Runners → New runner**, copy token

Trên build server:
```bash
cd github-runner
echo "RUNNER_TOKEN=YOUR_TOKEN" > .env
docker compose up -d
```

### Bước 4: Deploy

```bash
git add .
git commit -m "update"
git push
```

GitHub Actions tự động build và deploy lên ECS!

## Xem logs

```bash
aws logs tail /ecs/khoi-task --follow
```

## Xóa Infrastructure

```bash
cd terraform/ecs && terraform destroy
cd ../vpc && terraform destroy
```

---

Chi tiết đầy đủ xem tại [terraform/README.md](./terraform/README.md)
